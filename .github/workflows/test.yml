
on:
  push:
    branches:
      - main
  pull_request:

jobs:
  run-setup-go:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: setup-go
        id: setup-go
        uses: ./
      - run: |
          jq -r .GOPATH ${{ steps.setup-go.outputs.go-env }}
